# 文件名: .github/workflows/build-microros.yml
# 描述: v13 (最终版) 使用 micro-ROS 官方推荐的 "meta file" 机制来注入平台配置。
#       通过创建一个自定义的 CMake 元数据文件，我们直接、可靠地设置了裸机编译
#       所需的全部选项，解决了环境变量无法传递的问题，完成了最终的修复。

name: Build micro-ROS From Source (Final Version v13 - Custom Meta File)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y git cmake python3-pip gcc-arm-none-eabi

      - name: Install Python Dependencies
        run: |
          pip3 install -U colcon-common-extensions vcstool

      - name: Set up ROS 2 Humble
        uses: ros-tooling/setup-ros@v0.7
        with:
          required-ros-distributions: humble

      - name: Create, Build and Package micro-ROS
        shell: bash
        run: |
          source /opt/ros/humble/setup.bash

          echo "::group::1. Prepare and build the micro_ros_setup tool"
          mkdir -p microros_ws
          cd microros_ws

          git clone -b humble https://github.com/micro-ROS/micro_ros_setup.git src/micro_ros_setup
          
          for i in 1 2 3; do sudo rosdep init && break || sleep 5; done
          rosdep update
          
          colcon build --packages-select micro_ros_setup
          echo "::endgroup::"
          
          echo "::group::2. Download firmware sources and install their dependencies"
          source install/setup.bash

          ros2 run micro_ros_setup create_firmware_ws.sh generate_lib
          
          rosdep install --from-paths src --ignore-src -y
          echo "::endgroup::"
          
          echo "::group::3. Configure Platform and Build Firmware"
          # --- 【v13 最终修复核心】 ---

          # 1. 创建工具链文件 (保持不变)
          TOOLCHAIN_FILENAME="stm32_hardfloat.cmake"
          TOOLCHAIN_PATH="${PWD}/${TOOLCHAIN_FILENAME}"
          echo "ℹ️ Creating toolchain file: ${TOOLCHAIN_PATH}"
          echo '# Set the target system for ARM Cortex-M4 with Hard FPU
          set(CMAKE_SYSTEM_NAME Generic)
          set(CMAKE_SYSTEM_PROCESSOR arm)
          set(CMAKE_C_COMPILER arm-none-eabi-gcc)
          set(CMAKE_CXX_COMPILER arm-none-eabi-g++)
          set(TARGET_FLAGS "-mthumb -mcpu=cortex-m4 -mfloat-abi=hard -mfpu=fpv4-sp-d16 --specs=nosys.specs")
          set(CMAKE_C_FLAGS           "${TARGET_FLAGS} -Wall -Wextra" CACHE INTERNAL "")
          set(CMAKE_CXX_FLAGS         "${TARGET_FLAGS} -Wall -Wextra -fno-exceptions" CACHE INTERNAL "")
          set(CMAKE_ASM_FLAGS         "${TARGET_FLAGS}" CACHE INTERNAL "")
          set(CMAKE_EXE_LINKER_FLAGS  "-Wl,--gc-sections" CACHE INTERNAL "")
          ' > ${TOOLCHAIN_PATH}

          # 2. 【v13 变更】创建自定义元数据文件 (Custom Meta File)
          META_FILENAME="custom_meta.cmake"
          META_FILE_PATH="${PWD}/${META_FILENAME}"
          echo "ℹ️ Creating custom meta file for platform features: ${META_FILE_PATH}"
          echo '# Custom meta file for micro-ROS bare-metal configuration
          # This sets CMake variables to disable host-specific features like filesystems.
          set(RMW_UXRCE_TRANSPORT "custom" CACHE STRING "")
          set(UCLIENT_PROFILE_UDP OFF CACHE BOOL "")
          set(UCLIENT_PROFILE_TCP OFF CACHE BOOL "")
          set(BUILD_SHARED_LIBS OFF CACHE BOOL "")
          ' > ${META_FILE_PATH}
          
          echo "--- Verifying Toolchain & Meta Files ---"
          echo "--- Toolchain File ---"
          cat ${TOOLCHAIN_PATH}
          echo "--- Meta File ---"
          cat ${META_FILE_PATH}
          echo "----------------------------------------"

          # 3. 运行构建脚本，这次传递我们自己的元数据文件
          echo "🚀 Starting the build process using custom toolchain and meta files..."
          ros2 run micro_ros_setup build_firmware.sh ${TOOLCHAIN_PATH} ${META_FILE_PATH}
          echo "::endgroup::"

          echo "::group::4. Package the artifacts"
          LIB_PATH=$(find firmware/build -name "libmicroros.a" | head -n 1)
          if [ -z "$LIB_PATH" ]; then
            echo "❌ FATAL: libmicroros.a was NOT found after build."
            echo "--- Searching for CMakeError.log for post-mortem analysis ---"
            find firmware/build -name "CMakeError.log" -exec echo "Found log at: {}" \; -exec cat {} \;
            echo "--- Searching for failing package logs ---"
            find firmware/build -name "stderr_*.log" -exec echo "Found log at: {}" \; -exec cat {} \;
            echo "--- Listing the entire workspace for debugging ---"
            ls -R .
            exit 1
          else
            echo "✅ Success! Found library at: $LIB_PATH"
            cd ..
            mkdir -p export/lib
            mkdir -p export/include
            cp microros_ws/$LIB_PATH export/lib/
            cp -r microros_ws/firmware/build/include/* export/include/
            echo "✅ Artifacts copied to export/ directory for upload."
          fi
          echo "::endgroup::"

      - name: Upload Library Artifact
        uses: actions/upload-artifact@v4
        with:
          name: libmicroros_hard_float_from_source
          path: export/
